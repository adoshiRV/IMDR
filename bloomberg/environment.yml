name: imdrbbg
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pyarrow>=17.0
  - pyyaml>=6.0
  - jinja2>=3.1
  - python-dotenv>=1.0
  - pywin32  # Outlook COM automation for refresh summary emails
  - pip
  - pip:
      - --extra-index-url https://blpapi.bloomberg.com/repository/releases/python/simple/
      - blpapi
      - structlog>=24.0
