"""Production econ-domain helpers.

Shared modules used by `scripts/econ/{provider}/` prod fetchers. The
playground sandbox under `playground/econ/` keeps its own copies of these
helpers untouched -- prod and playground are intentionally decoupled.
"""
